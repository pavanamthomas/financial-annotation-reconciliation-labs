[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fsaslab"
version = "0.1.0"
description = "Audit-only checks on constructed financial spreadsheet annotations. An annotation that ties is not a complete total."
readme = "README.md"
license = { text = "MIT" }
authors = [
  { name = "Dr. Pavanam Thomas", email = "thomaspavanam@gmail.com" },
]
requires-python = ">=3.11"
dependencies = [
  "numpy",
  "pandas",
]
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Intended Audience :: Education",
  "Topic :: Office/Business :: Financial",
]

[project.optional-dependencies]
dev = ["pytest"]

[project.urls]
Homepage = "https://cursor.com/codebase/pavanam-thomas/fin-check"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
